declare global {
  namespace PrismaJson {
    // config json type example
    // type Task = {
    //   name: string;
    //   description?: string;
    //   isCompleted: boolean;
    // };
  }
}

export {};
